<div id="alerts" class="row">
    <!-- Alerts go here. -->
    <% if flash[:notice] %>
        <div data-alert class="alert-box slow-fade radius">
            <%= flash[:notice] %>
            <a href="#" class="close">&times;</a>
        </div>
    <% end %>
</div>

After signup, need to confirm.